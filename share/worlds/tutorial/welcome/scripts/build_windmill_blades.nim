# The blades are stuck! Make them turn.
# hint: `move me` first — it's a build, and you want to move it, not draw
# hint: things that happen over and over go inside a `forever:` loop
# hint: a small `sleep` between turns sets the speed
