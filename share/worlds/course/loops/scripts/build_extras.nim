# Extra challenges for kids who want more. No gates — bragging rights.
lock = true
speed = 0
color = white
box(vec3(0, 0, 0), vec3(0, 3, 0), color = white) # signpost

say "- Extra credit",
  """
  # Extra credit

  Finished everything? Try these — no prizes, just glory:

  - Give the dock a row of **pilings** (post, hop, repeat...).
  - Make your lighthouse **taller** — will the lamp follow? (No.
    But it looks great.)
  - Build a **spiral staircase** around the tower so you can walk up.
  - On Art Beach: a loop **inside** a loop. What does THAT make?
  """,
  width = 3.0
