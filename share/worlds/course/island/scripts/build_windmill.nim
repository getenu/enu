lock = true

# The windmill tower is baked furniture: its voxels live in the unit's data
# file (data/build_windmill/build_windmill.json), not in this script, so there's
# no build code here to distract from the exercise. It was authored by drawing
# the tower once and calling `persist()` to promote the blocks to saved edits.
#
# The player's exercise is next door in build_windmill_blades: give the stuck
# sails a `forever:` loop so they turn. The miller is very much hoping you will.
